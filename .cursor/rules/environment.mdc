---
description: Local machine environment - regenerated by agent-config-wizard on apply.
generated-by: agent-config-wizard
alwaysApply: true
---

# Environment

Generated by agent-config-wizard from the local machine. Manual edits are overwritten on apply.

## Runtime

- **OS:** Windows (Microsoft Windows [Version 10.0.26200.9445])
- **Shell:** PowerShell 5.1.26100.9444 (default for terminal commands)
- **Agent scratch directory:** `C:/Users/DanielKranich/.cursor/scratch`

## Tools on PATH

- `go` installed (go version go1.26.0 windows/amd64)
- `python` on PATH (C:\Users\DanielKranich\AppData\Local\Microsoft\WindowsApps\python.exe)
- `python3` on PATH (C:\Users\DanielKranich\AppData\Local\Microsoft\WindowsApps\python3.exe)
- `dotnet` on PATH (C:\Program Files\dotnet\dotnet.exe)
- `git` installed (git version 2.53.0.windows.1)
- `gh` installed (gh version 2.89.0 (2026-03-26))
- `docker` installed (Docker version 29.2.1, build a5c7197)
- `kubectl` installed (Client Version: v1.34.1)
- `gcloud` installed (Google Cloud SDK 564.0.0)
- `bash` installed (GNU bash, version 5.2.37(1)-release (x86_64-redhat-linux-gnu))
- `wsl` installed (WSL version: 2.7.14.0)
- `winget` installed (v1.29.290)
- `choco` installed (1.1.0)

## Absent runtimes

- Do not assume `node`, `npm`, or `npx`.
- Do not assume alternate JS package managers (`pnpm`, `yarn`, `bun`).
- Do not assume Python package tooling (`uv`).
- Do not assume Rust (`rustc`, `cargo`).
- Do not assume cloud CLIs (`aws`, `az`, `gcloud`).
- Do not assume IaC CLIs (`terraform`).
- Do not assume native build tools (`make`, `gcc`, `clang`).

## Paths

- Use Windows path syntax and PowerShell idioms when OS is Windows.
- Put ephemeral agent artifacts in the scratch directory, not the project root.
